/**
 * @file mola 常量
 * @author leon <ludafa@outlook.com>
 */

const PREFIX = 'mola';

export const EDITOR_HELPER_WINDOW = `${PREFIX}/WINDOW`;
export const EDITOR_HELPER_CONTAINER = `${PREFIX}/CONTAINER`;
export const EDITOR_HELPER_ATOM = `${PREFIX}/COMPONENT`;
export const EDITOR_HELPER_FIXED = `${PREFIX}/FIXED`;

export const WINDOW_WIDTH = 375;
export const BASE_FONT_SIZE = 16;
